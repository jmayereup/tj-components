import { a as j } from "./chunks/audio-utils-BwH2sOvH.js";
import { c as w } from "./chunks/tj-config-DtZWnqEi.js";
const S = '@import"https://fonts.googleapis.com/css2?family=Lato:wght@300;400;700&display=swap";:host{--tj-bg-color: #f8fafc;--tj-text-color: #1e293b;--tj-card-bg: #ffffff;--tj-accent-color: #b45309;--tj-subtitle-color: #64748b;--tj-card-border: #e2e8f0;--tj-btn-bg: #f1f5f9;--tj-btn-text: #475569;--tj-btn-hover: #e2e8f0;--tj-btn-slow-text: #2563eb;--tj-quiz-bg: #f8fafc;--tj-shadow: rgba(0, 0, 0, .05);--tj-input-bg: #ffffff;display:block;box-sizing:border-box;font-family:Lato,sans-serif;background-color:var(--tj-bg-color);color:var(--tj-text-color);overflow-anchor:auto;padding-bottom:2em;transition:background-color .3s,color .3s}:host(.dark-theme){--tj-bg-color: #0f172a;--tj-text-color: #e2e8f0;--tj-card-bg: #1e293b;--tj-card-border: #334155;--tj-accent-color: #fbbf24;--tj-subtitle-color: #94a3b8;--tj-btn-bg: #334155;--tj-btn-hover: #475569;--tj-btn-text: white;--tj-btn-slow-text: #93c5fd;--tj-quiz-bg: #334155;--tj-shadow: rgba(0, 0, 0, .5);--tj-input-bg: #1e293b}*,*:before,*:after{box-sizing:inherit}h1,h2,h3,p{margin:0;padding:0}h1,h2,h3{font-family:Lato,sans-serif}.book-header{max-width:56em;margin:0 auto;padding:1.5em 1em;display:flex;flex-direction:column;align-items:center;gap:1em}.theme-toggle,.print-toggle,.lang-swap{background:none;border:none;color:var(--tj-text-color);cursor:pointer;padding:.5em;border-radius:50%;transition:background-color .2s}.theme-toggle:hover,.print-toggle:hover,.lang-swap:hover{background-color:var(--tj-btn-hover)}.header-actions{display:flex;gap:.5em;justify-content:flex-end;width:100%;flex-wrap:wrap;margin-bottom:.5em}@media (max-width: 640px){.header-actions{justify-content:center;gap:.4em}.tj-speed-control{padding:.25em .4em .25em .6em;height:30px;font-size:.85em}.tj-speed-control:after{right:.5em}.tj-speed-select{padding-right:.9em}}.book-title{font-size:3em;line-height:1;color:var(--tj-accent-color);margin-bottom:.5em}.book-subtitle{font-size:1.25em;color:var(--tj-subtitle-color)}@media (max-width: 640px){.book-title{font-size:2em}.chapters-container{padding:1em 0}.chapter-card,.report-card-section{border-radius:12px;box-shadow:0 1px 4px #0000000f;padding:.5rem}}.chapters-container{margin:0 1rem;padding:.5rem .25rem}.chapters-container>*+*{margin-top:2rem}.chapter-card{background-color:var(--tj-card-bg);border:none;border-radius:16px;box-shadow:0 2px 8px #00000012,0 8px 24px #0000000a;transition:box-shadow .2s,background-color .3s;padding:.75rem;margin-bottom:2rem}.chapter-title{font-size:1.5em;line-height:1.3;color:var(--tj-accent-color);margin-bottom:1rem}.audio-controls{display:flex;gap:.75em;flex-wrap:wrap;align-items:center;margin-bottom:1em;justify-content:center}.audio-btn{display:flex;align-items:center;gap:.5em;padding:.45em .9em;border-radius:9999px;transition:all .2s ease;font-size:.875em;border-width:1px;border-style:solid;cursor:pointer;font-weight:600;touch-action:manipulation;-webkit-user-select:none;user-select:none;-webkit-tap-highlight-color:transparent}.audio-btn:active{transform:scale(.96)}.audio-btn-normal{background-color:var(--tj-btn-bg);color:var(--tj-btn-text);border-color:var(--tj-card-border)}.audio-btn-normal:hover{background-color:var(--tj-btn-hover)}.audio-btn-slow{background-color:var(--tj-btn-bg);color:var(--tj-btn-slow-text);border-color:var(--tj-card-border)}.audio-btn-slow:hover{background-color:var(--tj-btn-hover)}.audio-btn-cancel{background-color:var(--tj-btn-bg);color:var(--tj-btn-text);border-color:var(--tj-card-border)}.audio-btn-cancel:hover{background-color:var(--tj-btn-hover)}.chapter-text{color:var(--tj-text-color);line-height:1.625;margin-bottom:1.5em;font-size:1.125em}.chapter-text p{margin-bottom:1.25em}.translation-details{margin-bottom:2em}.translation-summary{display:flex;justify-content:space-between;align-items:center;font-weight:500;cursor:pointer;list-style:none;background-color:var(--tj-btn-bg);padding:.75em 1em;border-radius:10px;color:var(--tj-accent-color);transition:background-color .2s;border:none}.translation-summary:hover{background-color:var(--tj-btn-hover)}.translation-content{color:var(--tj-subtitle-color);font-style:italic;font-size:1em;margin-top:.75em;padding-left:.75em;padding-right:.75em}.quiz-container{background-color:transparent;padding:0;border-radius:0;border:none;margin-bottom:.5rem;margin-top:1.25rem}.quiz-title{font-size:1.25em;color:var(--tj-accent-color);margin-bottom:1rem;font-weight:700;display:flex;align-items:center;gap:.5em}.question-block{background:var(--tj-btn-bg);padding:.75rem;border-radius:10px;margin-bottom:.5rem}.question-text{margin-bottom:.5em;font-weight:600}.option-label{display:block;margin-bottom:.25em}input[type=radio]{accent-color:var(--tj-accent-color);transform:scale(1.2);margin-right:8px}.feedback{margin-top:8px;font-size:.9em;transition:all .3s}.check-btn{margin-top:.75em;background-color:var(--tj-accent-color);color:#fff;padding:.5em 1.25em;border-radius:8px;font-weight:700;transition:all .2s ease;border:none;cursor:pointer;touch-action:manipulation;-webkit-tap-highlight-color:transparent}.check-btn:hover{opacity:.88;transform:translateY(-1px)}.check-btn:active{transform:scale(.97);opacity:1}.lang-selector-container{margin:1.5em 0;text-align:center;width:100%}.lang-selector-label{font-weight:700;color:var(--tj-subtitle-color);margin-bottom:.75em;font-size:1.1em}.lang-selector-buttons{display:flex;justify-content:center;gap:1em;flex-wrap:wrap}.lang-btn{background:var(--tj-card-bg);border:2px solid var(--tj-card-border);color:var(--tj-text-color);padding:.75em 1.5em;border-radius:9999px;font-size:1em;font-weight:600;cursor:pointer;transition:all .2s}.lang-btn:hover{border-color:var(--tj-accent-color)}.lang-btn.active{background:var(--tj-accent-color);border-color:var(--tj-accent-color);color:#fff}.quiz-container.quiz-hidden-checked,.translation-details.translation-hidden-checked{display:none!important}.quiz-lock-message.visible{display:block;animation:fadeIn .3s ease-in-out}@keyframes fadeIn{0%{opacity:0}to{opacity:1}}details>summary::-webkit-details-marker{display:none}details[open] summary~*{animation:sweep .3s ease-in-out}@keyframes sweep{0%{opacity:0;transform:translateY(-10px)}to{opacity:1;transform:translateY(0)}}.chevron{transition:transform .3s;display:flex;align-items:center}details[open] .chevron{transform:rotate(180deg)}.playing{box-shadow:0 0 0 2px #60a5fa,0 0 0 3px var(--tj-bg-color)}.feedback-correct{color:#4ade80;font-weight:700}.feedback-wrong{color:#f87171;font-weight:700}.feedback-neutral{color:#9ca3af}.book-footer{max-width:48em;margin:0 auto;padding:1rem;text-align:left;font-size:.875em;color:var(--tj-subtitle-color)}.book-footer p{margin:.5em 0;line-height:1.5}.report-card-section{margin:0 1rem;background-color:var(--tj-card-bg);border:1px solid var(--tj-card-border);padding:2em;border-radius:12px;box-shadow:0 4px 6px -1px var(--tj-shadow);display:flex;flex-direction:column;gap:1.5em}.report-tally{font-weight:700;color:var(--tj-accent-color);text-align:center;font-size:1.25em}.student-inputs{display:grid;grid-template-columns:1fr 1fr;gap:1em}@media (max-width: 640px){.student-inputs{grid-template-columns:1fr}}.student-inputs input{padding:8px 12px;border:1px solid var(--tj-card-border);border-radius:6px;background:var(--tj-input-bg);color:var(--tj-text-color);font-size:.9em}.student-inputs input:disabled{opacity:.7;background:var(--tj-btn-bg);cursor:not-allowed}.report-actions{display:flex;gap:1em}.generate-btn{flex:3;background-color:var(--tj-accent-color);color:#fff;border:none;padding:.75em;border-radius:8px;font-weight:700;cursor:pointer;transition:opacity .2s;font-size:1em}.reset-btn{flex:1;background-color:#ef4444;color:#fff;border:none;padding:.75em;border-radius:8px;font-weight:700;cursor:pointer;transition:opacity .2s;font-size:1em}.generate-btn:hover,.reset-btn:hover{opacity:.9}.report-overlay{position:fixed;top:0;left:0;right:0;bottom:0;background:#0f172acc;-webkit-backdrop-filter:blur(8px);backdrop-filter:blur(8px);display:none;align-items:center;justify-content:center;z-index:2000;padding:1.5em}.report-overlay.visible{display:flex}.report-modal{background:var(--tj-card-bg);width:100%;max-width:550px;max-height:90vh;border-radius:1.5em;box-shadow:0 25px 50px -12px #00000040;display:flex;flex-direction:column;overflow:auto;border:1px solid var(--tj-card-border);position:relative}.rc-header{text-align:center;margin-bottom:24px}.rc-icon{font-size:2.5em;margin-bottom:8px}.rc-title{font-size:1.4em;font-weight:700;color:var(--tj-accent-color);margin-bottom:4px}.rc-subtitle{color:var(--tj-subtitle-color);font-weight:600;font-size:.9em;text-transform:uppercase;letter-spacing:.05em}.rc-student{background:var(--tj-card-bg);border:1px solid var(--tj-card-border);border-radius:12px;padding:16px;margin-bottom:24px;display:flex;justify-content:space-between;align-items:center}.rc-label{color:var(--tj-subtitle-color);font-size:.9em;font-weight:600}.rc-value{font-weight:700;color:var(--tj-text-color);text-align:right}.rc-number{color:var(--tj-subtitle-color);font-weight:500;font-size:.9em;display:block}.rc-score-row{display:flex;align-items:center;gap:20px;margin-bottom:16px}.rc-score-circle{width:80px;height:80px;border-radius:50%;background:var(--tj-btn-bg);color:var(--tj-accent-color);display:flex;flex-direction:column;align-items:center;justify-content:center;border:3px solid var(--tj-accent-color);flex-shrink:0}.rc-score-val{font-size:1.5em;font-weight:800;line-height:1}.rc-score-pct{font-size:.85em;font-weight:700;margin-top:2px}.rc-score-label{font-size:1.1em;font-weight:700;color:var(--tj-text-color)}.rc-bar-track{height:8px;background:var(--tj-btn-bg);border-radius:4px;overflow:hidden}.rc-bar-fill{height:100%;background:var(--tj-accent-color);border-radius:4px}.rc-details{background:var(--tj-btn-bg);padding:16px;border-radius:12px;margin-bottom:24px;font-size:.9em}.rc-detail-row{display:flex;justify-content:space-between;margin-bottom:8px}.rc-detail-row:last-child{margin-bottom:0}.rc-detail-row span:first-child{color:var(--tj-subtitle-color);font-weight:500}.rc-detail-row span:last-child{font-weight:600;color:var(--tj-text-color)}.rc-close-btn:hover{background:var(--tj-btn-bg)}.rc-submission-box{margin-top:20px;padding:20px;background:var(--tj-btn-bg);border-radius:12px;border:1px solid var(--tj-card-border);text-align:left;box-shadow:inset 0 2px 4px #00000005}.rc-submission-header{margin:0 0 12px;font-size:.85em;color:var(--tj-subtitle-color);font-weight:700;text-transform:uppercase;letter-spacing:.05em}.rc-teacher-code-input{width:100%;box-sizing:border-box;padding:12px 16px;border:2px solid var(--tj-card-border);border-radius:10px;font-size:1em;margin-bottom:8px;background:var(--tj-card-bg);transition:all .2s ease;outline:none;font-family:inherit;color:var(--tj-text-color)}.rc-teacher-code-input:focus{border-color:var(--tj-accent-color);box-shadow:0 0 0 4px #2563eb1a}.rc-help-text{margin:4px 0 0;font-size:.85em;color:var(--tj-subtitle-color);line-height:1.4}.rc-submit-btn{width:100%;padding:16px;background:var(--tj-accent-color);color:#fff;border:none;border-radius:12px;font-size:1.1em;font-weight:700;cursor:pointer;transition:all .2s ease;box-shadow:0 4px 6px #2563eb33;margin-bottom:8px}.rc-submit-btn:hover:not(:disabled){opacity:.9;transform:translateY(-1px);box-shadow:0 6px 12px #2563eb4d}.rc-submit-btn:disabled{opacity:.6;cursor:default;background:var(--tj-subtitle-color);box-shadow:none}.rc-secondary-btn{width:100%;padding:14px;background:var(--tj-btn-bg);color:var(--tj-text-color);border:1.5px solid var(--tj-card-border);border-radius:12px;font-size:1em;font-weight:600;cursor:pointer;transition:all .2s ease}.rc-secondary-btn:hover{background:var(--tj-btn-hover);border-color:var(--tj-subtitle-color)}@media print{:host{background:#fff!important;color:#000!important;padding:0!important}.book-header{padding-top:.5em!important;padding-bottom:.5em!important;page-break-after:avoid}.header-actions,.sticky-report,.book-footer,.audio-controls{display:none!important}.book-title{font-size:1.5em!important;color:#000!important;margin-bottom:.25em!important}.book-subtitle{font-size:.9em!important;margin-bottom:.5em!important;color:#666!important}.chapters-container{max-width:100%!important;padding:0!important;margin:0!important}.chapters-container>*+*{margin-top:.75em!important}.chapter-card{background:#fff!important;border:none!important;border-radius:0!important;box-shadow:none!important;padding:.25em 0!important;page-break-inside:avoid;margin-bottom:.35em!important;display:block!important}.chapter-title{font-size:.95em!important;color:#000!important;margin-bottom:.2em!important;font-weight:700!important;line-height:1.15!important}.chapter-text{font-size:.75em!important;line-height:1.3!important;margin-bottom:.4em!important;color:#000!important}.chapter-text p{margin-bottom:.3em!important}.translation-details{display:none!important}.quiz-container{background:#fff!important;border:none!important;padding:0!important;margin-top:.2em!important}.quiz-title{display:none!important}.question-block{background:#fff!important;border:none!important;padding:0!important;margin-bottom:.5em!important;break-inside:avoid}.question-text{font-size:.7em!important;margin-bottom:.1em!important;font-weight:600!important;color:#000!important;line-height:1.2!important}.option-label,input[type=radio],.check-btn,.feedback{display:none!important}@page{size:A4;margin:1cm}}.word{cursor:pointer;padding:0 2px;transition:background-color .2s,color .2s;border-radius:4px;display:inline-block}.word:hover{background-color:var(--tj-btn-hover);color:var(--tj-accent-color)}#voice-btn{background:var(--tj-btn-bg);border:1px solid var(--tj-card-border);border-radius:6px;padding:6px;color:var(--tj-btn-text);cursor:pointer;transition:background-color .2s;display:flex;align-items:center;justify-content:center}#voice-btn:hover{background-color:var(--tj-btn-hover)}.voice-overlay{position:fixed;top:0;left:0;right:0;bottom:0;background:#0f172ab3;-webkit-backdrop-filter:blur(4px);backdrop-filter:blur(4px);display:none;align-items:center;justify-content:center;z-index:1000;padding:1em}.voice-overlay.visible{display:flex}.voice-card{background:var(--tj-card-bg);width:100%;max-width:400px;max-height:80vh;border-radius:1.2em;box-shadow:0 20px 25px -5px #0003;display:flex;flex-direction:column;overflow:hidden;border:1px solid var(--tj-card-border)}.voice-card-header{padding:1.25em 1.5em;border-bottom:1px solid var(--tj-card-border);display:flex;justify-content:space-between;align-items:center}.voice-card-header h3{margin:0;font-size:1.25em;color:var(--tj-accent-color)}.close-voice-btn{background:none;border:none;color:var(--tj-text-color);font-size:1.5em;cursor:pointer;padding:.25em;line-height:1}.voice-list{padding:1em;overflow-y:auto;display:flex;flex-direction:column;gap:.5em}.voice-option-btn{display:flex;justify-content:space-between;align-items:center;padding:.75em 1em;border-radius:.75em;border:1px solid var(--tj-card-border);background:var(--tj-btn-bg);color:var(--tj-btn-text);cursor:pointer;transition:all .2s;text-align:left;font-size:.95em}.voice-option-btn:hover{background:var(--tj-btn-hover)}.voice-option-btn.active{background:var(--tj-accent-color);border-color:var(--tj-accent-color);color:#fff;font-weight:600}.voice-option-btn .badge{background:#fbbf24;color:#92400e;padding:2px 8px;border-radius:9999px;font-size:.75em;font-weight:700;text-transform:uppercase}@media (max-width: 640px){#voice-btn{padding:4px}}.browser-prompt-overlay{position:fixed;top:0;left:0;right:0;bottom:0;background:#0f172ae6;-webkit-backdrop-filter:blur(8px);backdrop-filter:blur(8px);display:none;align-items:center;justify-content:center;z-index:10000;color:#fff;padding:2em;text-align:center}.browser-prompt-card{background:#fff;color:#1e293b;padding:2.5em;border-radius:1.5em;max-width:400px;box-shadow:0 20px 25px -5px #0000001a,0 10px 10px -5px #0000000a}.browser-prompt-card h2{color:#b45309;margin-bottom:.5em;font-size:1.5em}.browser-prompt-card p{margin-bottom:1.5em;line-height:1.5}.browser-action-btn{display:inline-block;background-color:#ca8a04;color:#fff;padding:.75em 1.5em;border-radius:9999px;text-decoration:none;font-weight:700;transition:background-color .2s;cursor:pointer}.browser-action-btn:hover{background-color:#a16207}@media print{html.tj-print-scope body *{visibility:hidden!important}html.tj-print-scope tj-chapter-book[data-tj-print-target=true],html.tj-print-scope tj-chapter-book[data-tj-print-target=true] *{visibility:visible!important}html.tj-print-scope tj-chapter-book[data-tj-print-target=true]{position:absolute!important;left:0!important;top:0!important;width:100%!important}}.quiz-lock-message{font-size:.8em;color:var(--tj-subtitle-color);margin-top:.75em;padding:.5em 0}.chapter-card .quiz-container{margin-top:1.5em}.tj-toast-container{position:fixed;bottom:1.5em;left:50%;transform:translate(-50%);z-index:9999;display:flex;flex-direction:column-reverse;align-items:center;gap:.6em;pointer-events:none;width:calc(100% - 2em);max-width:420px}.tj-toast{display:flex;align-items:center;gap:.75em;padding:.85em 1.2em;border-radius:12px;font-size:.92em;font-weight:500;line-height:1.4;box-shadow:0 4px 24px #0000002e;pointer-events:auto;animation:tj-toast-in .3s cubic-bezier(.34,1.56,.64,1) forwards;width:100%;box-sizing:border-box}.tj-toast.hiding{animation:tj-toast-out .25s ease forwards}.tj-toast-icon{font-size:1.2em;flex-shrink:0}.tj-toast.success{background:#166534;color:#f0fdf4}.tj-toast.error{background:#991b1b;color:#fef2f2}.tj-toast.warning{background:#92400e;color:#fffbeb}.tj-toast.info{background:#1e3a8a;color:#eff6ff}@keyframes tj-toast-in{0%{opacity:0;transform:translateY(16px) scale(.96)}to{opacity:1;transform:translateY(0) scale(1)}}@keyframes tj-toast-out{0%{opacity:1;transform:translateY(0) scale(1)}to{opacity:0;transform:translateY(8px) scale(.96)}}.tj-confirm-overlay{position:fixed;top:0;right:0;bottom:0;left:0;background:#0f172ab3;backdrop-filter:blur(6px);-webkit-backdrop-filter:blur(6px);display:flex;align-items:center;justify-content:center;z-index:9998;padding:1.5em;animation:fadeIn .2s ease}.tj-confirm-card{background:var(--tj-card-bg);color:var(--tj-text-color);border-radius:20px;padding:2em 1.75em 1.5em;max-width:360px;width:100%;box-shadow:0 20px 40px #00000040;text-align:center;border:1px solid var(--tj-card-border)}.tj-confirm-icon{font-size:2.5em;margin-bottom:.4em}.tj-confirm-title{font-size:1.1em;font-weight:700;margin-bottom:.5em;color:var(--tj-text-color)}.tj-confirm-message{font-size:.9em;color:var(--tj-subtitle-color);line-height:1.5;margin-bottom:1.5em}.tj-confirm-actions{display:flex;gap:.75em}.tj-confirm-cancel{flex:1;padding:.75em;border-radius:10px;border:1.5px solid var(--tj-card-border);background:var(--tj-btn-bg);color:var(--tj-text-color);font-weight:600;font-size:.95em;cursor:pointer;transition:background .2s;font-family:inherit}.tj-confirm-cancel:hover{background:var(--tj-btn-hover)}.tj-confirm-ok{flex:1;padding:.75em;border-radius:10px;border:none;background:#ef4444;color:#fff;font-weight:700;font-size:.95em;cursor:pointer;transition:background .2s;font-family:inherit}.tj-confirm-ok:hover{background:#dc2626}.tj-toast.dictionary-lookup{flex-direction:column;align-items:stretch;padding:0;overflow:hidden;max-width:320px;background:var(--tj-card-bg);color:var(--tj-text-color);border:1px solid var(--tj-card-border)}.tj-toast-content{display:flex;flex-direction:column}.tj-toast-header{display:flex;align-items:center;gap:.75em;padding:.75em 1em;background:var(--tj-btn-bg);border-bottom:1px solid var(--tj-card-border)}.tj-toast-word{font-weight:700;font-size:1.1em;color:var(--tj-accent-color)}.tj-toast-body{padding:1em}.tj-toast-action-btn{width:100%;padding:.6em;border-radius:8px;border:none;background:var(--tj-accent-color);color:#fff;font-weight:600;cursor:pointer;transition:opacity .2s}.tj-toast-action-btn:hover{opacity:.9}.tj-toast-action-btn:disabled{opacity:.6;cursor:wait}.tj-toast-close{position:absolute;top:.5em;right:.5em;background:none;border:none;color:var(--tj-subtitle-color);cursor:pointer;font-size:1.1em;padding:4px;line-height:1;z-index:10}.tj-definition-container,.tj-translation-container{display:flex;flex-direction:column;gap:.5em;animation:fadeIn .3s ease}.tj-pos-badge{align-self:flex-start;padding:2px 8px;background:var(--tj-btn-bg);color:var(--tj-accent-color);border-radius:9999px;font-size:.75em;font-weight:700;text-transform:uppercase;letter-spacing:.05em}.tj-definition-text{font-size:.9em;line-height:1.5;margin:0}.tj-toast-error{color:#ef4444;font-size:.85em;font-weight:500}.tj-google-btn{display:inline-block;margin-top:.5em;font-size:.8em;color:var(--tj-accent-color);text-decoration:none;padding:4px 8px;border:1px solid var(--tj-card-border);border-radius:4px;background:var(--tj-btn-bg);font-weight:600;transition:all .2s;text-align:center}.tj-google-btn:hover{background:var(--tj-btn-hover);border-color:var(--tj-accent-color)}#audio-controls-placeholder{display:inline-flex;align-items:center;gap:.5em}.tj-speed-control{display:inline-flex;align-items:center;gap:.25em;background:var(--tj-btn-bg);border:1px solid var(--tj-card-border);border-radius:6px;padding:.25em .5em .25em .75em;height:32px;transition:background-color .2s,color .2s,border-color .2s;color:var(--tj-btn-text);position:relative;cursor:pointer}.tj-speed-control:hover{background:var(--tj-btn-hover);border-color:var(--tj-accent-color);color:var(--tj-accent-color)}.tj-speed-icon{flex-shrink:0;opacity:.85}.tj-speed-select{background:transparent;border:none;font-family:inherit;font-size:.9em;font-weight:700;color:inherit;cursor:pointer;outline:none;padding:0 1.2em 0 .2em;appearance:none;-webkit-appearance:none;-moz-appearance:none}.tj-speed-control:after{content:"";position:absolute;right:.8em;top:50%;transform:translateY(-50%);border-left:4px solid transparent;border-right:4px solid transparent;border-top:5px solid currentColor;pointer-events:none}.audio-btn-play{background-color:var(--tj-btn-bg);color:var(--tj-btn-slow-text);border-color:var(--tj-card-border)}.audio-btn-play:hover{background-color:var(--tj-btn-hover)}', q = `<header class="book-header">
    <div class="header-actions">
        <div id="audio-controls-placeholder"></div>
        <button id="print-toggle" class="print-toggle" aria-label="Print" title="Print friendly version">
            <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="6 9 6 2 18 2 18 9"></polyline><path d="M6 18H4a2 2 0 0 1-2-2v-5a2 2 0 0 1 2-2h16a2 2 0 0 1 2 2v5a2 2 0 0 1-2 2h-2"></path><rect x="6" y="14" width="12" height="8"></rect></svg>
        </button>
        <button id="theme-toggle" class="theme-toggle" aria-label="Toggle theme">
            <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"></path></svg>
        </button>
    </div>
    <h1 class="book-title" id="book-title"></h1>
    <p class="book-subtitle" id="book-subtitle"></p>
    <div id="lang-selector-placeholder"></div>
</header>

<div class="chapters-container" id="chapters-container" translate="no"></div>


<div class="report-card-section">
    <div class="report-tally">
        Score: <span id="score-tally">0</span> / <span id="total-tally">0</span>
    </div>
    <div class="student-inputs">
        <input type="text" id="student-name" placeholder="Jake" required>
        <input type="text" id="student-id" placeholder="01" required>
        <input type="text" id="student-homeroom" placeholder="1/1" required>
    </div>
    <div class="report-actions">
        <button class="generate-btn" id="generate-report">Generate Report Card</button>
        <button class="reset-btn" id="reset-book">Reset Quiz</button>
    </div>
</div>

<div class="report-overlay" id="report-overlay">
    <div class="report-modal" style="padding: 24px; border-radius: 16px;">
        <div id="report-content" class="report-area"></div>
    </div>
</div>
<footer class="book-footer">
    <p>Note: Please let me know if you see any errors or have any suggestions. I'm always looking to improve these materials.</p>
</footer>

<div class="voice-overlay" id="voice-overlay">
    <div class="voice-card">
        <div class="voice-card-header">
            <h3>Choose Voice</h3>
            <button class="close-voice-btn">×</button>
        </div>
        <div class="voice-list"></div>
    </div>
</div>

<div class="browser-prompt-overlay" id="browser-prompt-overlay">
    <div class="browser-prompt-card">
        <h2>Browser Support Needed</h2>
        <p>This application works best in standard browsers like <strong>Chrome</strong> or <strong>Safari</strong> to enable high-quality text-to-speech features.</p>
        <p>กรุณาเปิดใน Chrome หรือ Safari เพื่อใช้งานฟีเจอร์เสียงแบบเต็มรูปแบบ</p>
        <a class="browser-action-btn">Open in Browser</a>
        <button class="close-prompt" style="display: block; width: 100%; margin-top: 1.5em; border: none; background: transparent; text-decoration: underline; cursor: pointer; color: #64748b; font-weight: 600; font-size: 0.95em;" onclick="this.closest('.browser-prompt-overlay').style.display='none'">Continue anyway / ใช้งานต่อ</button>
    </div>
</div>
`;
class f extends HTMLElement {
  get code() {
    return this.getAttribute("code") !== null ? this.getAttribute("code") : w.teacherCode;
  }
  set code(t) {
    t != null ? this.setAttribute("code", t) : this.removeAttribute("code");
  }
  getLanguageName(t) {
    if (!t) return "Unknown";
    try {
      return new Intl.DisplayNames(["en"], { type: "language" }).of(t.split(/[-_]/)[0]);
    } catch {
      return t;
    }
  }
  static chapterHasTranslation(t) {
    return !t || typeof t.translation != "string" ? !1 : t.translation.trim().length > 0;
  }
  static bookHasAnyTranslations(t) {
    return !t || !Array.isArray(t.chapters) ? !1 : t.chapters.some((o) => f.chapterHasTranslation(o));
  }
  static ensureGlobalPrintScoping() {
    f._globalPrintScopingReady || (f._globalPrintScopingReady = !0, window.addEventListener("beforeprint", () => {
      let t = document.querySelector('tj-chapter-book[data-tj-print-target="true"]');
      t || (t = document.querySelector('tj-chapter-book[print-scope="component"]'), t && t.setAttribute("data-tj-print-target", "true")), t && document.documentElement.classList.add("tj-print-scope");
    }), window.addEventListener("afterprint", () => {
      document.documentElement.classList.remove("tj-print-scope"), document.querySelectorAll('tj-chapter-book[data-tj-print-target="true"]').forEach((t) => {
        t.removeAttribute("data-tj-print-target");
      });
    }));
  }
  constructor() {
    var o;
    super(), this.attachShadow({ mode: "open" }), this.synth = window.speechSynthesis, this.currentUtterance = null, this.currentButtonId = null, this.isTextSwapped = !1, this.studentInfo = { nickname: "", number: "", homeroom: "", teacherCode: "" }, this.submissionUrl = (o = w) == null ? void 0 : o.submissionUrl, this.isSubmitting = !1, this.ttsState = {
      status: "idle",
      activeButtonId: null,
      activeElementId: null,
      activeRate: 1,
      activeLang: null
    }, this._ttsActionSeq = 0, this._ttsUtteranceSeq = 0, this.language = "fr-FR", this.selectedVoiceName = null, this.totalScore = 0, this.totalQuestions = 0, this.absoluteTotalQuestions = 0, this.wrongQuestions = [], this.lockoutTimers = /* @__PURE__ */ new Map();
    const t = parseFloat(localStorage.getItem("tj-chapter-book-speed"));
    this.playbackSpeed = isNaN(t) ? 0.7 : t;
  }
  connectedCallback() {
    this.synth && this.synth.onvoiceschanged !== void 0 && (this.synth.onvoiceschanged = () => this._updateVoiceList()), this._initVisibilityObserver();
    const t = this.getAttribute("src");
    requestAnimationFrame(() => {
      this.config ? typeof this.config == "object" ? this.render(this.config) : this._parseAndRender(String(this.config)) : this.hasAttribute("config") ? this._parseAndRender(this.getAttribute("config")) : t ? this.loadData(t) : this.shadowRoot.querySelector('script[type="application/json"]') ? this._parseAndRender(this.shadowRoot.querySelector('script[type="application/json"]').textContent) : this._parseAndRender(this.textContent);
    });
  }
  _parseAndRender(t) {
    try {
      if (!t || !t.trim()) return;
      const o = JSON.parse(t.trim());
      this.render(o);
    } catch (o) {
      console.error("Error parsing inline JSON data", o), this.shadowRoot.innerHTML = '<p style="color: red;">Error loading book data: Invalid JSON.</p>';
    }
  }
  async loadData(t) {
    try {
      const e = await (await fetch(t)).json();
      this.render(e);
    } catch (o) {
      console.error("Error loading chapter data:", o), this.shadowRoot.innerHTML = '<p style="color: red;">Error loading book data.</p>';
    }
  }
  _getBestVoice(t) {
    return j(window.speechSynthesis, t);
  }
  _showVoiceOverlay() {
    const t = this.shadowRoot.querySelector(".voice-overlay");
    t && (t.classList.add("visible"), document.body.style.overflow = "hidden", this._updateVoiceList());
  }
  _hideVoiceOverlay() {
    const t = this.shadowRoot.querySelector(".voice-overlay");
    t && (t.classList.remove("visible"), document.body.style.overflow = "");
  }
  _updateVoiceList() {
    if (!this.synth) return;
    const t = this.synth.getVoices(), o = this.shadowRoot.querySelector(".voice-list"), e = this.shadowRoot.querySelector("#voice-btn");
    if (!o || !e || t.length === 0) return;
    const r = this.language, n = r.split(/[-_]/)[0].toLowerCase(), a = t.filter((i) => i.lang.split(/[-_]/)[0].toLowerCase() === n), c = this._getBestVoice(r);
    !this.selectedVoiceName && c && (this.selectedVoiceName = c.name), o.innerHTML = "", a.sort((i, p) => i.name.localeCompare(p.name)), a.forEach((i) => {
      const p = document.createElement("button");
      p.classList.add("voice-option-btn"), this.selectedVoiceName === i.name && p.classList.add("active");
      let l = `<span>${i.name}</span>`;
      c && i.name === c.name && (l += '<span class="badge">Best</span>'), p.innerHTML = l, p.onclick = () => {
        this.selectedVoiceName = i.name, this.cancelTTS(), this._updateVoiceList(), this._hideVoiceOverlay();
      }, o.appendChild(p);
    });
  }
  _initVisibilityObserver() {
    this._visibilityObserver = new IntersectionObserver((t) => {
      t.forEach((o) => {
        const e = o.target, r = e.id, n = e.querySelector(`#quiz-${r}`);
        !o.isIntersecting && o.boundingClientRect.bottom < 0 && n && n.dataset.checked === "true" && !n.classList.contains("quiz-hidden-checked") && this._hideCheckedQuiz(e, n);
      });
    }, {
      threshold: 0,
      rootMargin: "0px"
    });
  }
  _hideCheckedQuiz(t, o) {
    const e = t.querySelector(".translation-details"), r = t.querySelector(".quiz-lock-message");
    o.offsetHeight, o.classList.add("quiz-hidden-checked"), e && (e.classList.add("translation-hidden-checked"), e.open = !1), r && (r.innerHTML = "Results Hidden"), console.log(`Hidden checked quiz for chapter ${t.id}`);
  }
  render(t) {
    if (!t) return;
    this.hasAnyTranslations = f.bookHasAnyTranslations(t), this.absoluteTotalQuestions = 0, t.chapters && t.chapters.forEach((i) => {
      i.quiz && (this.absoluteTotalQuestions += i.quiz.length);
    }), t.language ? (this.language = t.language, this.originalLanguage = t.language) : this.originalLanguage = this.language, t.translationLanguage ? (this.translationLanguage = t.translationLanguage, this.originalTranslationLanguage = t.translationLanguage) : (this.translationLanguage = this.language.startsWith("en") ? "th-TH" : "en-US", this.originalTranslationLanguage = this.translationLanguage), this.shadowRoot.innerHTML = `
            <style>${S}</style>
            ${q}
        `;
    const o = this.shadowRoot.getElementById("book-title");
    o && (o.textContent = t.title);
    const e = this.shadowRoot.getElementById("book-subtitle");
    e && (e.textContent = t.subtitle);
    const r = this.shadowRoot.getElementById("audio-controls-placeholder");
    r && this.shouldShowAudioControls() && (r.innerHTML = `
                <div class="tj-speed-control" title="Playback Speed">
                    <svg class="tj-speed-icon" viewBox="0 0 24 24" width="18" height="18" fill="currentColor">
                        <path d="M20.38 8.57l-1.23 1.85a8 8 0 0 1-.22 7.58H5.07A8 8 0 0 1 12 6a7.89 7.89 0 0 1 6 2.73l1.42-1.42A9.91 9.91 0 0 0 12 4a10 10 0 0 0-7.68 16.4h15.36A10 10 0 0 0 20.38 8.57zM10 12a2 2 0 1 0 4 0 2 2 0 0 0-4 0zm3-6h-2v4h2V6z"/>
                    </svg>
                    <select id="speed-select" class="tj-speed-select">
                        <option value="0.5">0.5x</option>
                        <option value="0.6">0.6x</option>
                        <option value="0.7">0.7x</option>
                        <option value="0.8">0.8x</option>
                        <option value="0.9">0.9x</option>
                        <option value="1.0">1.0x</option>
                        <option value="1.2">1.2x</option>
                    </select>
                </div>
                <button id="voice-btn" title="Choose Voice">
                    <svg viewBox="0 0 24 24" width="20" height="20" fill="currentColor">
                        <path d="M9 13c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm0-6c1.1 0 2 .9 2 2s-.9 2-2 2-2-.9-2-2 .9-2 2-2zm0 8c-2.67 0-8 1.34-8 4v2h16v-2c0-2.66-5.33-4-8-4zm-6 4c.22-.72 3.31-2 6-2 2.7 0 5.77 1.29 6 2H3zM15.08 7.05c.84 1.18.84 2.71 0 3.89l1.68 1.69c2.02-2.02 2.02-5.17 0-7.27l-1.68 1.69zM18.42 3.7l-1.7 1.71c2.3 2 2.3 5.6 0 7.6l1.7 1.71c3.28-3.23 3.28-8.15 0-11.02z"/>
                    </svg>
                </button>
            `);
    const n = this.shadowRoot.getElementById("lang-selector-placeholder");
    n && this.hasAnyTranslations && (n.innerHTML = `
                <div class="lang-selector-container">
                    <p class="lang-selector-label">I want to read in:</p>
                    <div class="lang-selector-buttons">
                        <button class="lang-btn ${this.isTextSwapped ? "" : "active"}" data-action="set-lang" data-swap="false">${this.getLanguageName(this.originalLanguage)}</button>
                        <button class="lang-btn ${this.isTextSwapped ? "active" : ""}" data-action="set-lang" data-swap="true">${this.getLanguageName(this.originalTranslationLanguage)}</button>
                    </div>
                </div>
            `);
    const a = this.shadowRoot.getElementById("chapters-container");
    a && (a.innerHTML = t.chapters ? t.chapters.map((i, p) => this.renderChapter(i, p)).join("") : "<p>No chapters found.</p>");
    const c = this.shadowRoot.getElementById("total-tally");
    c && (c.textContent = this.absoluteTotalQuestions), this.attachEventListeners(), this._updateVoiceList(), this.checkBrowserSupport(), this.shadowRoot.querySelectorAll(".chapter-card").forEach((i) => {
      this._visibilityObserver && this._visibilityObserver.observe(i);
    });
  }
  _getAndroidIntentLink() {
    if (!/android/i.test(navigator.userAgent)) return "";
    const e = new URL(window.location.href).toString().replace(/^https?:\/\//, ""), r = window.location.protocol.replace(":", "");
    return `intent://${e}#Intent;scheme=${r};package=com.android.chrome;end`;
  }
  checkBrowserSupport() {
    if (!this.shouldShowAudioControls()) {
      const t = this.shadowRoot.querySelector(".browser-prompt-overlay");
      if (t) {
        t.style.display = "flex";
        const o = this._getAndroidIntentLink(), e = this.shadowRoot.querySelector(".browser-action-btn");
        o ? (e.href = o, e.textContent = "Open in Chrome") : (e.innerHTML = 'Use Safari / Chrome<br><span style="font-size: 0.8em; font-weight: normal;">กรุณาเปิดใน Safari หรือ Chrome</span>', e.onclick = (r) => {
          r.preventDefault(), this._showToast("Please open this page in Safari or Chrome for audio features.", "info", 5e3);
        });
      }
    }
  }
  shouldShowAudioControls() {
    if (!window.speechSynthesis) return !1;
    const t = navigator.userAgent.toLowerCase();
    return !(t.includes("wv") || t.includes("webview") || t.includes("instagram") || t.includes("facebook") || t.includes("line"));
  }
  renderChapter(t, o) {
    const e = `text-${t.id}`, r = `quiz-${t.id}`, n = `trans-${t.id}`, a = f.chapterHasTranslation(t), c = this.wrapWords(t.content ?? t.text), i = a ? this.wrapWords(t.translation) : "", p = i, l = t.quiz.map((m, v) => `
            <div class="question-block">
                <p class="question-text">${m.question}</p>
                ${m.options.map((y) => `
                    <label class="option-label">
                        <input type="radio" name="${t.id}-q${v}" value="${y.value}"> ${y.text}
                    </label>
                `).join("")}
                <p class="feedback"></p>
            </div>
        `).join(""), d = '<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polygon points="5 3 19 12 5 21 5 3"></polygon></svg>', s = '<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="6" y="6" width="12" height="12" rx="2" ry="2"></rect></svg>', u = '<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"></circle><line x1="2" y1="12" x2="22" y2="12"></line><path d="M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z"></path></svg>', h = '<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="6 9 12 15 18 9"></polyline></svg>', b = '<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M17 3a2.828 2.828 0 1 1 4 4L7.5 20.5 2 22l1.5-5.5L17 3z"></path></svg>';
    let g;
    if (this.shouldShowAudioControls())
      g = `
                <div class="audio-controls">
                    <button data-action="play" data-target="${e}" id="btn-${t.id}-play" class="audio-btn audio-btn-play">
                        <span class="icon-wrapper">${d}</span> Play
                    </button>
                    <button data-action="cancel-tts" id="btn-${t.id}-cancel" class="audio-btn audio-btn-cancel" aria-label="Cancel audio" title="Stop audio">
                        <span class="icon-wrapper">${s}</span> Cancel
                    </button>
                </div>`;
    else {
      const m = /android/i.test(navigator.userAgent);
      let v = "";
      if (m) {
        const x = window.location.href.replace(/^https?:\/\//, ""), k = window.location.protocol.replace(":", "");
        v = `<div style="margin-top: 0.5em;"><a href="${`intent://${x}#Intent;scheme=${k};package=com.android.chrome;end`}" style="color: var(--tj-accent-color); text-decoration: underline; font-weight: bold;">Open in Chrome</a></div>`;
      }
      g = `
                <div style="background-color: var(--tj-btn-bg); color: var(--tj-subtitle-color); padding: 0.75em; border-radius: 0.5em; border: 1px dashed var(--tj-card-border); text-align: center; font-size: 0.9em; margin-bottom: 1em;">
                    <p style="margin-bottom: 0.25em;">🎧 Audio available in Chrome or Safari</p>
                    <p>เสียงบรรยายใช้ได้ใน Chrome หรือ Safari</p>
                    ${v}
                </div>`;
    }
    return `
            <section id="${t.id}" class="chapter-card">
                <h2 class="chapter-title">${t.title}</h2>
                
                ${g}

                <template data-tj-template="main-original">${c}</template>
                ${a ? `<template data-tj-template="main-translation">${i}</template>` : ""}
                <template data-tj-template="trans-original">${c}</template>
                ${a ? `<template data-tj-template="trans-translation">${i}</template>` : ""}

                <div id="${e}" class="chapter-text">
                    ${this.isTextSwapped && a ? p : c}
                </div>

                ${a ? `
                <aside id="trans-aside-${t.id}" style="display: none;">
                <details class="translation-details group">
                    <summary class="translation-summary">
                        <span style="display: flex; align-items: center; gap: 0.5rem;">${u} Translation</span>
                        <span class="chevron">${h}</span>
                    </summary>
                    <div style="padding: 0.5em 0.75em 0;">
                        <button data-action="play" data-target="${n}" data-lang="${this.translationLanguage}" id="btn-trans-${t.id}" class="audio-btn audio-btn-play" style="font-size: 0.8em; padding: 0.25em 0.5em;">
                            <span class="icon-wrapper">${d}</span> Play
                        </button>
                    </div>
                    <div id="${n}" class="translation-content">
                        ${this.isTextSwapped ? c : i}
                    </div>
                </details>
                </aside>
                ` : ""}

                <div class="quiz-container" id="${r}">
                    <h3 class="quiz-title">${b} Comprehension</h3>
                    ${l}
                    <button data-action="check-quiz" data-target="${r}" class="check-btn">Check</button>
                </div>
                <div id="lock-msg-${t.id}" class="quiz-lock-message" style="display: none;">
                    Answers will disappear when you scroll past.
                </div>
            </section>
        `;
  }
  attachEventListeners() {
    f.ensureGlobalPrintScoping();
    const t = this.shadowRoot.querySelector("#print-toggle");
    t && t.addEventListener("click", () => {
      document.querySelectorAll('tj-chapter-book[data-tj-print-target="true"]').forEach((l) => {
        l.removeAttribute("data-tj-print-target");
      }), this.setAttribute("data-tj-print-target", "true"), document.documentElement.classList.add("tj-print-scope"), window.print();
    }), this.shadowRoot.querySelectorAll(".lang-btn").forEach((l) => {
      l.addEventListener("click", (d) => {
        const s = l.dataset.swap === "true";
        if (this.isTextSwapped !== s) {
          this.cancelTTS();
          const u = this.language;
          this.language = this.translationLanguage, this.translationLanguage = u, this.isTextSwapped = s, this.applyLanguageTextSwap(), this.shadowRoot.querySelectorAll('button[id^="btn-trans-"][data-action="play"]').forEach((h) => {
            h.dataset.lang = this.translationLanguage;
          }), this.selectedVoiceName = null, this._updateVoiceList(), this.shadowRoot.querySelectorAll(".lang-btn").forEach((h) => {
            h.classList.toggle("active", h.dataset.swap === String(this.isTextSwapped));
          }), this.resetApp(!0);
        }
      });
    });
    const o = this.shadowRoot.querySelector("#voice-btn");
    o && o.addEventListener("click", () => {
      this._showVoiceOverlay();
    });
    const e = this.shadowRoot.getElementById("speed-select");
    e && (e.value = this.playbackSpeed.toString(), e.addEventListener("change", (l) => {
      this.playbackSpeed = parseFloat(l.target.value), localStorage.setItem("tj-chapter-book-speed", l.target.value);
    }));
    const r = this.shadowRoot.querySelector(".close-voice-btn");
    r && r.addEventListener("click", () => {
      this._hideVoiceOverlay();
    });
    const n = this.shadowRoot.querySelector(".voice-overlay");
    n && n.addEventListener("click", (l) => {
      l.target === n && this._hideVoiceOverlay();
    });
    const a = this.shadowRoot.querySelector("#theme-toggle");
    a && a.addEventListener("click", () => {
      this.classList.toggle("dark-theme");
      const l = this.classList.contains("dark-theme"), d = '<svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="5"></circle><line x1="12" y1="1" x2="12" y2="3"></line><line x1="12" y1="21" x2="12" y2="23"></line><line x1="4.22" y1="4.22" x2="5.64" y2="5.64"></line><line x1="18.36" y1="18.36" x2="19.78" y2="19.78"></line><line x1="1" y1="12" x2="3" y2="12"></line><line x1="21" y1="12" x2="23" y2="12"></line><line x1="4.22" y1="19.78" x2="5.64" y2="18.36"></line><line x1="18.36" y1="5.64" x2="19.78" y2="4.22"></line></svg>', s = '<svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"></path></svg>';
      a.innerHTML = l ? d : s;
    }), this.shadowRoot.querySelectorAll('button[data-action="play"]').forEach((l) => {
      l.addEventListener("click", (d) => {
        const s = d.target.closest("button"), u = s.dataset.target, h = this.playbackSpeed;
        this.playAudio(u, h, s.id);
      });
    }), this.shadowRoot.querySelectorAll('button[data-action="cancel-tts"]').forEach((l) => {
      l.addEventListener("click", () => {
        this.cancelTTS();
      });
    }), this.shadowRoot.querySelectorAll('button[data-action="check-quiz"]').forEach((l) => {
      l.addEventListener("click", (d) => {
        const s = d.target.closest("button"), u = s.dataset.target;
        this.checkRadioAnswers(u, s);
      });
    });
    const c = this.shadowRoot.querySelector("#generate-report");
    c && c.addEventListener("click", () => this.showReportCard());
    const i = this.shadowRoot.querySelector("#reset-book");
    i && i.addEventListener("click", () => this.resetApp());
    const p = this.shadowRoot.querySelector(".close-report-btn");
    p && p.addEventListener("click", () => this.hideReportCard()), this.shadowRoot.querySelectorAll(".chapter-text, .translation-content").forEach((l) => {
      l.addEventListener("click", (d) => {
        const s = d.target.closest(".word");
        if (s) {
          let u = this.language;
          l.classList.contains("translation-content") && !this.isTextSwapped ? u = this.translationLanguage : l.classList.contains("chapter-text") && this.isTextSwapped ? u = this.language : l.classList.contains("translation-content") && this.isTextSwapped && (u = this.translationLanguage), this.playWord(s.innerText, u), this._showDictionaryToast(s.innerText, u);
        }
      });
    }), this.shadowRoot.querySelectorAll(".translation-details").forEach((l) => {
      l.addEventListener("toggle", (d) => {
        const s = l.closest(".chapter-card");
        s && this.handleTranslationToggle(s.id, l.open);
      });
    });
  }
  wrapWords(t) {
    const o = (r) => /[\u0E00-\u0E7F]/.test(r);
    return (Array.isArray(t) ? t : [t]).map((r) => {
      if (r == null) return "";
      const n = r.replace(/<[^>]*>/g, "");
      if (o(n)) {
        if (typeof Intl < "u" && Intl.Segmenter) {
          const c = new Intl.Segmenter("th", { granularity: "word" }).segment(n), i = [];
          for (const p of c)
            p.isWordLike ? i.push(`<span class="word">${p.segment}</span>`) : i.push(p.segment);
          return `<p>${i.join("")}</p>`;
        }
        return `<p>${r}</p>`;
      } else
        return `<p>${n.split(/(\s+)/).map((i) => /\s+/.test(i) || i === "" ? i : `<span class="word">${i}</span>`).join("")}</p>`;
    }).join("");
  }
  updateIcon(t, o) {
    const e = this.shadowRoot.querySelector(`#${t}`);
    if (!e) return;
    const r = e.querySelector(".icon-wrapper"), n = '<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polygon points="5 3 19 12 5 21 5 3"></polygon></svg>', a = '<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="6" y="4" width="4" height="16"></rect><rect x="14" y="4" width="4" height="16"></rect></svg>';
    o === "play" ? (r.innerHTML = n, e.classList.remove("playing")) : o === "pause" && (r.innerHTML = a, e.classList.add("playing"));
  }
  resetAllButtons() {
    this.shadowRoot.querySelectorAll('button[data-action="play"]').forEach((t) => {
      this.updateIcon(t.id, "play");
    });
  }
  cancelTTS() {
    try {
      this.synth && this.synth.cancel();
    } finally {
      this._ttsActionSeq++, this._ttsUtteranceSeq++, this.ttsState.status = "idle", this.ttsState.activeButtonId = null, this.ttsState.activeElementId = null, this.ttsState.activeRate = 1, this.ttsState.activeLang = null, this.currentButtonId = null, this.currentUtterance = null, this.resetAllButtons();
    }
  }
  applyLanguageTextSwap() {
    this.shadowRoot.querySelectorAll("section.chapter-card").forEach((t) => {
      const o = t.querySelector('.chapter-text[id^="text-"]'), e = t.querySelector('.translation-content[id^="trans-"]');
      if (!o || !e) return;
      const r = t.querySelector(this.isTextSwapped ? 'template[data-tj-template="main-translation"]' : 'template[data-tj-template="main-original"]'), n = t.querySelector(this.isTextSwapped ? 'template[data-tj-template="trans-original"]' : 'template[data-tj-template="trans-translation"]');
      r && (o.innerHTML = r.innerHTML), n && (e.innerHTML = n.innerHTML);
    });
  }
  handleTranslationToggle(t, o) {
  }
  playAudio(t, o, e) {
    if (!(this.ttsState.activeElementId === t && this.ttsState.status !== "idle")) {
      this.cancelTTS(), this.startNewSpeech(t, o, e);
      return;
    }
    if (this.ttsState.status === "playing") {
      const n = (() => {
        const c = this.shadowRoot.querySelector(`#${e}`);
        return c && c.dataset.lang ? c.dataset.lang : this.language;
      })();
      if (this.ttsState.activeRate !== o || this.ttsState.activeLang !== n) {
        this.cancelTTS(), this.startNewSpeech(t, o, e);
        return;
      }
      this.pauseTTS(e);
      return;
    }
    if (this.ttsState.status === "paused") {
      const n = (() => {
        const c = this.shadowRoot.querySelector(`#${e}`);
        return c && c.dataset.lang ? c.dataset.lang : this.language;
      })();
      if (this.ttsState.activeRate !== o || this.ttsState.activeLang !== n) {
        this.cancelTTS(), this.startNewSpeech(t, o, e);
        return;
      }
      this.resumeTTS(t, o, e);
      return;
    }
    this.startNewSpeech(t, o, e);
  }
  pauseTTS(t) {
    this._ttsActionSeq++;
    try {
      this.synth && this.synth.pause();
    } catch (o) {
      console.warn("Speech pause() failed:", o);
    }
    this.ttsState.status = "paused", this.ttsState.activeButtonId = t, this.resetAllButtons();
  }
  resumeTTS(t, o, e) {
    this._ttsActionSeq++;
    const r = this._ttsActionSeq;
    this.ttsState.status = "playing", this.ttsState.activeButtonId = e, this.ttsState.activeElementId = t, this.ttsState.activeRate = o, this.updateIcon(e, "pause");
    try {
      this.synth && this.synth.resume();
    } catch (n) {
      console.warn("Speech resume() failed:", n);
    }
    window.setTimeout(() => {
      if (this._ttsActionSeq !== r) return;
      const n = !!(this.synth && this.synth.paused), a = !!(this.synth && this.synth.speaking);
      this.ttsState.status === "playing" && (n || !a) && this.startNewSpeech(t, o, e);
    }, 650);
  }
  startNewSpeech(t, o, e) {
    const r = (navigator.userAgent || navigator.vendor || window.opera).toLowerCase();
    if (r.includes("wv") || r.includes("webview") || r.includes("instagram") || r.includes("facebook") || r.includes("line")) {
      this.showTTSError(e);
      return;
    }
    const n = this.shadowRoot.querySelector(`#${t}`);
    if (!n) return;
    const a = n.innerText, c = this.shadowRoot.querySelector(`#${e}`), i = c && c.dataset.lang ? c.dataset.lang : this.language;
    this._ttsActionSeq++, this._ttsUtteranceSeq++;
    const p = this._ttsUtteranceSeq;
    this.ttsState.status = "playing", this.ttsState.activeButtonId = e, this.ttsState.activeElementId = t, this.ttsState.activeRate = o, this.ttsState.activeLang = i;
    try {
      this.currentUtterance = new SpeechSynthesisUtterance(a);
      let d = this.synth.getVoices().find((h) => h.name === this.selectedVoiceName);
      const s = d ? d.lang.split(/[-_]/)[0].toLowerCase() : null, u = i.split(/[-_]/)[0].toLowerCase();
      (!d || s !== u) && (d = this._getBestVoice(i)), this.currentUtterance.lang = i, d && (this.currentUtterance.voice = d), this.currentUtterance.rate = o, this.currentUtterance.onend = () => {
        this._ttsUtteranceSeq === p && (this.updateIcon(e, "play"), this._ttsActionSeq++, this.ttsState.status = "idle", this.ttsState.activeButtonId = null, this.ttsState.activeElementId = null, this.ttsState.activeRate = 1, this.ttsState.activeLang = null, this.currentButtonId = null, this.currentUtterance = null);
      }, this.currentUtterance.onerror = (h) => {
        this._ttsUtteranceSeq === p && (console.error("Speech error:", h), this.updateIcon(e, "play"), this._ttsActionSeq++, this.ttsState.status = "idle", this.ttsState.activeButtonId = null, this.ttsState.activeElementId = null, this.ttsState.activeRate = 1, this.ttsState.activeLang = null, this.currentButtonId = null, h.error !== "canceled" && h.error !== "interrupted" && this.showTTSError(e));
      }, this.currentButtonId = e, this.updateIcon(e, "pause"), this.synth.speak(this.currentUtterance);
    } catch (l) {
      console.error("Speech synthesis setup error", l), this.showTTSError(e), this.updateIcon(e, "play"), this._ttsActionSeq++, this.ttsState.status = "idle", this.ttsState.activeButtonId = null, this.ttsState.activeElementId = null, this.ttsState.activeRate = 1, this.ttsState.activeLang = null;
    }
  }
  playWord(t, o) {
    if (!this.synth) return;
    const e = t.replace(/[.,\/#!$%\^&\*;:{}=\-_`~()]/g, "").trim();
    if (!e) return;
    this.synth.cancel();
    const r = new SpeechSynthesisUtterance(e);
    let a = this.synth.getVoices().find((p) => p.name === this.selectedVoiceName);
    const c = a ? a.lang.split(/[-_]/)[0].toLowerCase() : null, i = (o || this.language).split(/[-_]/)[0].toLowerCase();
    (!a || c !== i) && (a = this._getBestVoice(o || this.language)), r.lang = o || this.language, a && (r.voice = a), r.rate = 0.8, this.synth.speak(r);
  }
  showTTSError(t) {
    const o = `
            <div class="tts-error-message" style="background-color: #fee2e2; color: #991b1b; padding: 0.75em; border-radius: 0.5em; border: 1px solid #f87171; text-align: center; font-weight: 500;">
                <p style="margin-bottom: 0.5em;">⚠️ Audio not supported in this browser</p>
                <p style="font-size: 0.9em;">Please open in Chrome or Safari</p>
                <p style="font-size: 0.9em;">กรุณาเปิดใน Chrome หรือ Safari</p>
            </div>
        `;
    if (t) {
      const a = this.shadowRoot.querySelector(`#${t}`);
      if (a) {
        const c = a.closest(".audio-controls");
        if (c) {
          c.innerHTML = o;
          return;
        }
      }
    }
    if (this.shadowRoot.querySelector(".tts-error-message")) return;
    const e = document.createElement("div");
    e.className = "tts-error-message", e.style.cssText = `
            background-color: #fee2e2;
            color: #991b1b;
            padding: 1em;
            margin: 1em auto;
            border-radius: 0.5em;
            border: 1px solid #f87171;
            max-width: 48em;
            text-align: center;
            font-weight: 500;
        `, e.innerHTML = `
            <p>Text-to-speech is not supported in this browser. Please try opening this page in a standard browser like Chrome or Safari.</p>
            <p style="margin-top: 0.5em;">เบราว์เซอร์นี้ไม่รองรับการอ่านออกเสียง กรุณาลองเปิดหน้านี้ในเบราว์เซอร์มาตรฐาน เช่น Chrome หรือ Safari</p>
            <button style="margin-top: 0.5em; background: none; border: none; color: #991b1b; text-decoration: underline; cursor: pointer;">Dismiss / ปิด</button>
        `;
    const r = e.querySelector("button");
    r.onclick = () => e.remove();
    const n = this.shadowRoot.querySelector(".book-header");
    n ? n.after(e) : this.prepend(e);
  }
  checkRadioAnswers(t, o) {
    const e = this.shadowRoot.querySelector(`#${t}`), r = e.closest(".chapter-card"), n = e.querySelectorAll(".question-block");
    let a = 0, c = 0, i = !0;
    if (n.forEach((s) => {
      s.querySelector('input[type="radio"]:checked') || (i = !1);
    }), !i) {
      this._showToast("Please answer all questions before checking.", "warning");
      return;
    }
    o && (o.disabled = !0, o.textContent = "Checked", o.style.opacity = "0.7", o.style.cursor = "not-allowed"), e.dataset.checked = "true", n.forEach((s) => {
      const u = s.querySelector('input[type="radio"]:checked'), h = s.querySelector(".feedback");
      if (s.querySelectorAll('input[type="radio"]').forEach((g) => g.disabled = !0), c++, h.classList.remove("feedback-correct", "feedback-wrong", "feedback-neutral"), u.value === "correct")
        h.textContent = "Correct !", h.classList.add("feedback-correct"), a++;
      else {
        h.textContent = "Incorrect.", h.classList.add("feedback-wrong");
        const g = r ? r.querySelector(".chapter-title").innerText : "Unknown Chapter", m = s.querySelector(".question-text").innerText;
        this.wrongQuestions.push({
          question: m,
          chapter: g
        });
      }
    }), this.updateScore(a, c);
    const p = t.replace("quiz-", ""), l = this.shadowRoot.querySelector(`#lock-msg-${p}`), d = r.querySelector(`#trans-aside-${p}`);
    d && (d.style.display = "block"), l && (l.innerHTML = "Answers and translation will disappear when you scroll past.", l.classList.add("visible"), l.style.display = "block");
  }
  updateScore(t, o) {
    this.totalScore = (this.totalScore || 0) + t;
    const e = this.shadowRoot.querySelector("#score-tally"), r = this.shadowRoot.querySelector("#total-tally");
    e && r && (e.textContent = this.totalScore, r.textContent = this.absoluteTotalQuestions);
  }
  showReportCard() {
    const t = this.shadowRoot.querySelector("#student-name"), o = this.shadowRoot.querySelector("#student-id"), e = this.shadowRoot.querySelector("#student-homeroom"), r = t.value.trim(), n = o.value.trim(), a = e.value.trim();
    if (!r || !n) {
      this._showToast("Please enter both Student Name and Student ID before generating a report card.", "warning"), r ? o.focus() : t.focus();
      return;
    }
    this.studentInfo = { ...this.studentInfo, nickname: r, number: n, homeroom: a }, t.disabled = !0, o.disabled = !0, e && (e.disabled = !0);
    const c = this.shadowRoot.querySelector(".report-overlay"), i = this.shadowRoot.querySelector("#report-content"), p = /* @__PURE__ */ new Date(), l = p.toLocaleDateString(), d = p.toLocaleTimeString([], { hour: "2-digit", minute: "2-digit" }), s = this.shadowRoot.querySelector(".book-title").innerText, u = this.absoluteTotalQuestions > 0 ? Math.round(this.totalScore / this.absoluteTotalQuestions * 100) : 0, h = u >= 80 ? "🏆" : u >= 50 ? "⭐" : "💪", b = u >= 80 ? "Excellent!" : u >= 50 ? "Good effort!" : "Keep practicing!";
    i.innerHTML = `
            <div class="rc-header">
                <div class="rc-icon">📄</div>
                <div class="rc-title">${s}</div>
                <div class="rc-subtitle">Report Card</div>
            </div>
            <div class="rc-student">
                <span class="rc-label">Student</span>
                <span class="rc-value">${r} <span class="rc-number">(${n}) ${a ? `- ${a}` : ""}</span></span>
            </div>
            <div class="rc-score-row">
                <div class="rc-score-circle">
                    <div class="rc-score-val">${u}%</div>
                    <div class="rc-score-pct">Overall</div>
                </div>
                <div class="rc-score-label">${h} ${b}</div>
            </div>
            <div class="rc-bar-track" style="margin: 0 0 16px 0;"><div class="rc-bar-fill" style="width:${u}%"></div></div>
            <div class="rc-details">
                <div class="rc-detail-row"><span>Total Score</span><span>${this.totalScore} / ${this.absoluteTotalQuestions}</span></div>
                <div class="rc-detail-row"><span>Date</span><span>${l}</span></div>
                <div class="rc-detail-row"><span>Time</span><span>${d}</span></div>
            </div>

            ${this.wrongQuestions.length > 0 ? `
                <div class="report-wrong-list" style="margin-bottom: 20px;">
                    <h3 style="font-size: 1em; color: var(--tj-error-color); margin-bottom: 10px; font-weight: 700;">Needs Review:</h3>
                    <div style="max-height: 200px; overflow-y: auto; background: var(--tj-btn-bg); border-radius: 8px; border: 1px solid var(--tj-card-border);">
                    ${this.wrongQuestions.map((v) => `
                        <div class="report-wrong-item" style="padding: 10px; border-bottom: 1px solid var(--tj-card-border);">
                            <span class="report-wrong-chapter" style="font-size: 0.75em; color: var(--tj-subtitle-color); text-transform: uppercase;">${v.chapter}</span>
                            <div style="font-size: 0.9em;">${v.question}</div>
                        </div>
                    `).join("")}
                    </div>
                </div>
            ` : ""}

            <div class="rc-submission-box">
                <p class="rc-submission-header">Submission (Optional)</p>
                <input type="text" id="report-teacher-code" class="rc-teacher-code-input" placeholder="Enter Teacher Code" value="${this.studentInfo.teacherCode || ""}">
                <p class="rc-help-text">Enter the teacher code to submit, or take a screenshot of this page.</p>
            </div>

            <div class="rc-actions" style="margin-top: 16px;">
                <button class="rc-submit-btn" id="submit-score-btn">Submit Score Online</button>
                <button class="rc-secondary-btn close-report-btn">Close Report</button>
            </div>
        `;
    const g = i.querySelector("#submit-score-btn");
    g && (g.onclick = () => this._submitScore());
    const m = i.querySelector(".close-report-btn");
    m && (m.onclick = () => this.hideReportCard()), c.classList.add("visible");
  }
  hideReportCard() {
    this.shadowRoot.querySelector(".report-overlay").classList.remove("visible");
  }
  async _submitScore() {
    const t = this.shadowRoot.querySelector("#report-teacher-code"), o = t ? t.value.trim() : this.studentInfo.teacherCode;
    if (this.studentInfo.teacherCode = o, o !== this.code) {
      this._showToast("Invalid Teacher Code. Please show your report card screenshot to your teacher.", "error");
      return;
    }
    if (this.isSubmitting) return;
    const e = this.shadowRoot.querySelector("#submit-score-btn");
    if (!e) return;
    const r = e.textContent;
    this.isSubmitting = !0, e.textContent = "Submitting...", e.disabled = !0;
    const n = this.shadowRoot.querySelector(".book-title").innerText, a = this.absoluteTotalQuestions > 0 ? Math.round(this.totalScore / this.absoluteTotalQuestions * 100) : 0, c = {
      nickname: this.studentInfo.nickname,
      homeroom: this.studentInfo.homeroom || "",
      studentId: this.studentInfo.number,
      quizName: "Book- " + n,
      score: a,
      total: 100
    };
    try {
      await fetch(this.submissionUrl, {
        method: "POST",
        mode: "no-cors",
        headers: {
          "Content-Type": "application/json"
        },
        body: JSON.stringify(c)
      }), this._showToast("Score successfully submitted! ✓", "success"), e.textContent = "Submitted ✓", e.style.background = "var(--tj-subtitle-color)";
    } catch (i) {
      console.error("Error submitting score:", i), this._showToast("There was an error submitting your score. Please try again.", "error"), e.textContent = r, e.disabled = !1, this.isSubmitting = !1;
    }
  }
  async resetApp(t = !1) {
    if (!t && !await this._showConfirmModal(
      "⚠️ Reset Progress",
      "Are you sure you want to reset everything? Your scores and progress will be lost."
    ))
      return;
    this.totalScore = 0, this.wrongQuestions = [];
    const o = this.shadowRoot.querySelector("#student-name"), e = this.shadowRoot.querySelector("#student-id");
    o && (o.disabled = !1, o.value = ""), e && (e.disabled = !1, e.value = "");
    const r = this.shadowRoot.querySelector("#score-tally"), n = this.shadowRoot.querySelector("#total-tally");
    r && (r.textContent = "0"), n && (n.textContent = this.absoluteTotalQuestions), this.shadowRoot.querySelectorAll(".chapter-card").forEach((a) => {
      const c = `quiz-${a.id}`, i = a.querySelector(`#${c}`);
      if (i) {
        i.classList.remove("quiz-hidden-checked", "locked-open", "locked-delay"), delete i.dataset.checked;
        const d = i.querySelector('button[data-action="check-quiz"]');
        d && (d.disabled = !1, d.textContent = "Check", d.style.opacity = "1", d.style.cursor = "pointer"), i.querySelectorAll('input[type="radio"]').forEach((s) => {
          s.disabled = !1, s.checked = !1;
        }), i.querySelectorAll(".feedback").forEach((s) => {
          s.textContent = "", s.className = "feedback";
        });
      }
      const p = a.querySelector(".translation-details");
      p && (p.classList.remove("translation-hidden-checked"), p.open = !1);
      const l = a.querySelector(".quiz-lock-message");
      l && (l.classList.remove("visible"), l.textContent = "");
    }), this.lockoutTimers.forEach((a) => clearInterval(a)), this.lockoutTimers.clear(), window.scrollTo({ top: 0, behavior: "smooth" });
  }
  disconnectedCallback() {
    this.synth && this.synth.cancel(), this._visibilityObserver && this._visibilityObserver.disconnect();
  }
  _showToast(t, o = "info", e = 3500) {
    let r = this.shadowRoot.querySelector(".tj-toast-container");
    r || (r = document.createElement("div"), r.className = "tj-toast-container", this.shadowRoot.appendChild(r));
    const n = { success: "✓", error: "✕", warning: "⚠", info: "ℹ" }, a = document.createElement("div");
    a.className = `tj-toast ${o}`, a.innerHTML = `<span class="tj-toast-icon">${n[o] || "ℹ"}</span><span>${t}</span>`, r.appendChild(a), setTimeout(() => {
      a.classList.add("hiding"), a.addEventListener("animationend", () => a.remove(), { once: !0 });
    }, e);
  }
  _showConfirmModal(t, o) {
    return new Promise((e) => {
      const r = document.createElement("div");
      r.className = "tj-confirm-overlay", r.innerHTML = `
                <div class="tj-confirm-card">
                    <div class="tj-confirm-title">${t}</div>
                    <div class="tj-confirm-message">${o}</div>
                    <div class="tj-confirm-actions">
                        <button class="tj-confirm-cancel">Cancel</button>
                        <button class="tj-confirm-ok">Reset</button>
                    </div>
                </div>
            `, this.shadowRoot.appendChild(r);
      const n = (a) => {
        r.style.animation = "tj-toast-out 0.2s ease forwards", r.addEventListener("animationend", () => r.remove(), { once: !0 }), e(a);
      };
      r.querySelector(".tj-confirm-cancel").onclick = () => n(!1), r.querySelector(".tj-confirm-ok").onclick = () => n(!0), r.addEventListener("click", (a) => {
        a.target === r && n(!1);
      });
    });
  }
  async fetchWordData(t, o) {
    var e, r;
    try {
      const a = ((s) => {
        const u = {
          english: "en",
          spanish: "es",
          french: "fr",
          german: "de",
          thai: "th",
          en: "en",
          es: "es",
          fr: "fr",
          de: "de",
          th: "th"
        }, h = s.split(/[-_]/)[0].toLowerCase();
        return u[h] || h || "en";
      })(o), c = `https://freedictionaryapi.com/api/v1/entries/${a}/${encodeURIComponent(t)}`, i = await fetch(c);
      if (i.ok) {
        const s = await i.json(), u = s && typeof s == "object" && s.entries && s.entries.length > 0, h = Array.isArray(s) && ((r = (e = s[0]) == null ? void 0 : e.meanings) == null ? void 0 : r.length) > 0;
        if (u || h)
          return { type: "dictionary", content: s };
      }
      const p = `https://translate.googleapis.com/translate_a/single?client=gtx&sl=${a}&tl=en&dt=t&q=${encodeURIComponent(t)}`, d = await (await fetch(p)).json();
      return d && d[0] && d[0][0] ? { type: "translation", content: d[0][0][0] } : null;
    } catch (n) {
      return console.error("Lookup failed", n), null;
    }
  }
  _showDictionaryToast(t, o) {
    const e = t.replace(/[.,\/#!$%\^&\*;:{}=\-_`~()]/g, "").trim();
    if (!e) return;
    let r = this.shadowRoot.querySelector(".tj-toast-container");
    r || (r = document.createElement("div"), r.className = "tj-toast-container", this.shadowRoot.appendChild(r)), r.querySelectorAll(".tj-toast.dictionary-lookup").forEach((d) => d.remove());
    const n = document.createElement("div");
    n.className = "tj-toast info dictionary-lookup", n.innerHTML = `
            <div class="tj-toast-content">
                <div class="tj-toast-header">
                    <span class="tj-toast-icon">📖</span>
                    <span class="tj-toast-word">${e}</span>
                </div>
                <div class="tj-toast-body">
                    <button class="tj-toast-action-btn">Explore Word</button>
                </div>
            </div>
            <button class="tj-toast-close">✕</button>
        `, r.appendChild(n);
    const a = n.querySelector(".tj-toast-action-btn"), c = n.querySelector(".tj-toast-body"), i = n.querySelector(".tj-toast-close");
    let p = !1, l = setTimeout(() => {
      p || (n.classList.add("hiding"), n.addEventListener("animationend", () => n.remove(), { once: !0 }));
    }, 5e3);
    i.onclick = (d) => {
      d.stopPropagation(), clearTimeout(l), n.classList.add("hiding"), n.addEventListener("animationend", () => n.remove(), { once: !0 });
    }, a.onclick = async (d) => {
      if (d.stopPropagation(), p) return;
      p = !0, clearTimeout(l), a.disabled = !0, a.textContent = "Looking up...";
      const s = await this.fetchWordData(e, o);
      if (!s)
        c.innerHTML = '<span class="tj-toast-error">Could not find definition.</span>';
      else if (s.type === "dictionary") {
        const u = Array.isArray(s.content), h = u ? s.content[0] : s.content.entries[0], b = h.meanings ? h.meanings[0] : h.senses ? h.senses[0] : null, g = (b == null ? void 0 : b.partOfSpeech) || (u ? h.meanings[0].partOfSpeech : "word"), m = u ? h.meanings[0].definitions[0].definition : h.senses ? h.senses[0].definition : h.meanings[0].definitions[0].definition;
        c.innerHTML = `
                    <div class="tj-definition-container">
                        <span class="tj-pos-badge">${g}</span>
                        <p class="tj-definition-text">${m}</p>
                        <a href="https://www.google.com/search?q=define+${encodeURIComponent(e)}" target="_blank" class="tj-google-btn">Try Google</a>
                    </div>
                `;
      } else
        c.innerHTML = `
                    <div class="tj-translation-container">
                        <span class="tj-pos-badge">translation</span>
                        <p class="tj-definition-text">${s.content}</p>
                        <a href="https://translate.google.com/?sl=auto&tl=en&text=${encodeURIComponent(e)}&op=translate" target="_blank" class="tj-google-btn">Try Google</a>
                    </div>
                `;
      setTimeout(() => {
        n.isConnected && (n.classList.add("hiding"), n.addEventListener("animationend", () => n.remove(), { once: !0 }));
      }, 1e4);
    };
  }
}
customElements.define("tj-chapter-book", f);
